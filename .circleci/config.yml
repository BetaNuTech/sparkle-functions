version: 2

jobs:
  build:
    docker:
      - image: circleci/node:6.11
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: yarn
          working_directory: functions

  test:
    docker:
      - image: circleci/node:6.11
    steps:
      - checkout
      - run:
          name: Unit Tests
          command: ls && yarn test-unit
          working_directory: functions
      - run:
          name: End to End Tests
          command: yarn test-e2e
          working_directory: functions

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
