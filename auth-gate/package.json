{
  "name": "sparkle-auth-gate",
  "description": "Firebase Auth blocking functions for Sparkle: refuses sign-ups that were never invited",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "mocha test/**/*.test.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.1.1"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "mocha": "^10.8.2"
  }
}
